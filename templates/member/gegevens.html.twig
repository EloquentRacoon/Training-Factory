{% extends 'member.html.twig' %}
{%  block content %}
    <div class="col m-5">
        {{ form_start(registrationForm) }}
        {{ form_widget(registrationForm) }}


        <button class="btn btn-lg btn-primary btn-block mb-3 mt-5" type="submit">
            Register
        </button>
        {{ form_end(registrationForm) }}
    </div>
{% endblock %}